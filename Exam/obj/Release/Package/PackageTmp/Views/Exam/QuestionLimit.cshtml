@section Styles{
    <link href="~/Content/Selectize/css/selectize.css" rel="stylesheet" />
}
@section Scripts{
    <script src="~/Content/Selectize/js/standalone/selectize.js"></script>
    <script src="~/Scripts/custom/questionLimit.js"></script>
}


<div class="department-container">
    <div class="col-md-12" id="1">
        <select id="select1" class="select">
            <option></option>
            @foreach (var item in (List<Tuple<int, string>>)ViewBag.Departments)
            {
                <option value="@item.Item1">@item.Item2</option>
            }
        </select>
    </div>
</div>
<hr />
<div class="category-container" style="">
    <div class="col-md-12">
        <select id="parentCategory" class="">
            <option></option>
            @foreach (var item in (List<Tuple<int, string, int>>)ViewBag.ParentCategories)
            {
                <option value="@item.Item1">@item.Item2</option>
            }
        </select>
    </div>
    <div class="col-md-12" id="sub">

    </div>
</div>
<div class="">
    <div class="form-group">
        <div class="col-md-1">
            <input id="count" type="number" class="form-control" placeholder="Count" />
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-1">
            <input id="limit" type="number" class="form-control" placeholder="Limit" />
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-2">
            <a id="save" class="btn btn-success btn-icon icon-left">
                <i class="entypo-list-add"></i>
                Save
            </a>
        </div>
    </div>
</div>